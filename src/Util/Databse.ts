import { GuildLoggingSettings } from '@prisma/client'

export default class extends null {
    // public static async getGuildLoggingSettings(id: string): Promise<GuildLoggingSettings> {
    //     return await prisma.guildLoggingSettings.findFirst({ where: { id: guildId } }) ??
    //     await prisma.guildLoggingSettings.create({ data: { id: guildId } })
    // }
}